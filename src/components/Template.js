import React from 'react'
import { Link } from 'react-router-dom'

function Template() {
	return (
		<div>
			{/* This is a template file, rename function name and export to component name */}
		</div>
	)
}

export default Template
